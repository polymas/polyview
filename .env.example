# 前端配置
VITE_PORT=8010
VITE_API_TARGET=http://localhost:8002
# 如果前后端不在同一服务器，设置完整后端地址，例如：
# VITE_API_TARGET=http://your-backend-server:8002
# 或者如果前端直接调用后端（不使用proxy），设置：
# VITE_API_BASE_URL=http://your-backend-server:8002

# 后端配置（通过start.py启动时使用）
HOST=0.0.0.0
BACKEND_PORT=8002
FRONTEND_PORT=8010
API_TARGET=http://0.0.0.0:8002
